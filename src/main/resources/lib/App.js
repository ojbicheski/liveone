'use strict';

/* App Module */

//var myApp = angular.module('LiveOneApp', ['angularFileUpload']);
var myApp = angular.module('LiveOneApp', []);
		
/*
var myApp = angular.module('LiveOneApp', ['ngRoute']).
  config(['$routeProvider', function($routeProvider) {
  $routeProvider.
      when('/member/basic', 
    		  { templateUrl: 'member/tabMember.html', controller: 'MemberCtrl' }).
      when('/member/preferences', 
    		  { templateUrl: 'member/tabMemberPreferences.html', controller: 'MemberCtrl' }).
      when('/member/addresses', 
    		  { templateUrl: 'member/tabMemberAddresses.html', controller: 'MemberAddressCtrl' }).
      when('/member/phones', 
    		  { templateUrl: 'member/tabMemberPhones.html', controller: 'MemberPhoneCtrl' }).
      otherwise({redirectTo: '/member/basic'});
}]);
*/
